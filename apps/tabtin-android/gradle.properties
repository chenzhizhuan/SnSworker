org.gradle.jvmargs=-Xmx4g -XX:+UseG1GC
# 项目路径含中文（Windows 上 AGP 9 默认拒绝），按官方指引放行
android.overridePathCheck=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# 避免 IDE/Gradle 注入 testOnly，导致安装失败后又报 "Activity does not exist"
android.injected.testOnly=false

android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Debug 包默认连接 api-test。需要临时连本地 dev 栈时，在命令行显式传：
# -PDEV_HOST=10.0.2.2 -PDEV_PORT=6060 -PDEV_WEB_BASE_URL=http://10.0.2.2:5176
