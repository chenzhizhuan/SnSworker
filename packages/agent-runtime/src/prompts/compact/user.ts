/**
 * Compact 路径——全量 LLM 摘要的 user 指令。
 *
 * SSoT 由本文件持有。来源为旧 `compact/compact.ts:51-71` 的 `COMPACT_USER_PROMPT`
 * inline 常量；按宪法 v0.1 §3.1 资源化。
 *
 * 9 sections 结构与 incremental compact / auto-condense 的 8 sections 对齐
 * （后者无"Active Files State"，因为增量场景下文件状态由前次 summary 维持）。
 *
 * 语言：中文（2026-05-20 决策 4 全中文化）。
 */
export const COMPACT_USER_PROMPT = [
  '请总结目前为止的对话。你的摘要必须包含以下所有部分：',
  '',
  '1. **用户请求**：每一条明确的用户请求及其当前状态（待处理 / 进行中 / 已完成）',
  '2. **关键决策**：重要的技术决策、其理由，以及考虑过的替代方案',
  '3. **文件与代码**：查看 / 修改 / 创建过的所有文件，附关键代码片段',
  '   - 包含确切的文件路径以及改了什么',
  '   - 对修改过的文件：改了什么、为什么改',
  '4. **工具结果**：重要的工具输出、错误信息和搜索结果',
  '5. **错误与修复**：遇到的问题、定位到的根因，以及如何解决',
  '6. **当前状态**：本次摘要之前正在进行的工作',
  '7. **后续步骤**：用户明确要求的待办任务（一条都不要漏）',
  '8. **活动文件状态**：所有正在处理的文件的当前状态（已读 / 已改 / 已建）',
  '9. **重要上下文**：发现的任何领域知识、约束或约定',
  '10. **已尝试清单（防复读）**：列出所有已执行过的检索 / 搜索 / 扫描类命令'
    + '（尤其是 `run_terminal_command`），以及它们的结果（有结果 / 无结果 / 报错）。'
    + '务必保留"某个关键词已搜过但无结果"这一事实——这是防止摘要后模型'
    + '重复执行同一命令的关键记忆。',
  '',
  '重要：',
  '- 保留确切的文件路径、函数名、变量名和错误信息',
  '- 保留继续工作所需的代码片段',
  '- 不要把具体细节泛化成模糊的陈述',
  '- 不要调用任何工具。只回复摘要正文本身。',
].join('\n');
